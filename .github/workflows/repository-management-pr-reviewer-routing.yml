---
name: 'Repos: PR Routing'

# Design invariants -- do not regress these without a very good reason:
#  - schedule + workflow_dispatch only. Never pull_request/pull_request_target:
#    this run reads pull request data with a privileged cross-repo app token, and
#    that token must never be exposed to a run triggered by untrusted fork content.
#  - Offset crons ('7,22,37,52 * * * *' plus a daily '13 3 * * *' full-sweep
#    backstop) rather than a round-minute mark, because schedule runs are
#    best-effort and can be delayed or skipped near :00/:15/:30/:45.
on:
  schedule:
    - cron: '7,22,37,52 * * * *'
    - cron: '13 3 * * *'
  workflow_dispatch:
    inputs:
      pull_request_url:
        description: 'Route a single pull request (URL or number). Leave blank to sweep all open pull requests.'
        default: ''
        type: string
      updated_within_minutes:
        description: 'Only sweep pull requests updated within the last N minutes. Use 0 for every open pull request.'
        default: '60'
        type: string
      what_if:
        description: 'Compute and log the routing decisions without calling gh pr edit.'
        default: true
        type: boolean
permissions:
  contents: read

concurrency:
  group: repository-management-pr-reviewer-routing
  cancel-in-progress: false

defaults:
  run:
    shell: pwsh

jobs:
  route:
    name: Route pull request reviewers and labels
    if: github.repository == 'Azure/azure-verified-modules-tools'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    environment: avm
    steps:
      - name: Checkout trusted tools
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install Avm.Authoring
        run: |
          $ErrorActionPreference = 'Stop'
          $maxAttempts = 3
          for ($attempt = 1; $attempt -le $maxAttempts; $attempt++) {
            try {
              Install-PSResource -Name Avm.Authoring -Scope CurrentUser -TrustRepository -ErrorAction Stop
              break
            }
            catch {
              if ($attempt -eq $maxAttempts) {
                throw
              }
              $delaySeconds = 5 * [math]::Pow(2, $attempt - 1)
              Write-Warning "Avm.Authoring installation failed on attempt $attempt of ${maxAttempts}: $($_.Exception.Message). Retrying in $delaySeconds seconds."
              Start-Sleep -Seconds $delaySeconds
            }
          }
          Import-Module Avm.Authoring

      - name: Create scoped reviewer routing token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.AVM_APP_CLIENT_ID }}
          private-key: ${{ secrets.AVM_APP_PRIVATE_KEY }}
          owner: Azure
          repositories: bicep-registry-modules
          permission-pull-requests: write
          permission-members: read

      - name: Route pull request reviewers and labels
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          EVENT_NAME: ${{ github.event_name }}
          EVENT_SCHEDULE: ${{ github.event.schedule }}
          PULL_REQUEST_URL: ${{ inputs.pull_request_url }}
          UPDATED_WITHIN_MINUTES: ${{ inputs.updated_within_minutes }}
          WHAT_IF: ${{ inputs.what_if }}
        run: |
          Import-Module Avm.Authoring -Force -ErrorAction Stop

          $whatIf = $env:WHAT_IF -eq 'true'

          # workflow_dispatch has no `github.event.schedule`, so the lookback
          # comes from the dispatch input there. A `schedule` trigger has no
          # inputs (an empty UPDATED_WITHIN_MINUTES would otherwise cast to
          # 0, turning every cadence run into a full sweep) so the lookback
          # is mapped explicitly from the cron that fired, failing fast on
          # an unrecognized cron rather than silently defaulting.
          $updatedWithinMinutes = switch ($env:EVENT_NAME) {
            'workflow_dispatch' { [int]$env:UPDATED_WITHIN_MINUTES }
            'schedule' {
              switch ($env:EVENT_SCHEDULE) {
                '7,22,37,52 * * * *' { 60 }
                '13 3 * * *' { 0 }
                default { throw "Unexpected schedule '$($env:EVENT_SCHEDULE)' for the PR reviewer routing workflow." }
              }
            }
            default { throw "Unexpected event '$($env:EVENT_NAME)' triggered the PR reviewer routing workflow." }
          }

          Write-Output "Pull request URL override: $($env:PULL_REQUEST_URL)"
          Write-Output "Updated within minutes: $updatedWithinMinutes"
          Write-Output "What if: $whatIf"

          ./repository-management/reviewer-routing/scripts/Invoke-AvmPrReviewerRouting.ps1 `
            -Repository 'Azure/bicep-registry-modules' `
            -PullRequestUrl $env:PULL_REQUEST_URL `
            -UpdatedWithinMinutes $updatedWithinMinutes `
            -WhatIf:$whatIf

          $global:LASTEXITCODE = 0
